{#
    Deprecated / superseded.

    The database-query-cache profiler diagnostics now render inside Symfony's
    built-in Cache panel via the `DatabaseCacheDataCollector` decorator of
    `data_collector.cache`, backed by the bundle override
    templates/bundles/WebProfilerBundle/Collector/cache.html.twig. This file is
    no longer referenced by any data_collector tag and kept only as a marker.
#}
