{% set url = content.url ?? '' %}
{% set ratio = content.aspectRatio ?? 'original' %}
<div class="iikiti-social-embed" data-aspect-ratio="{{ ratio|e }}">
    {% if not url %}
        <span class="iikiti-social-embed--placeholder">Social URL missing.</span>
    {% else %}
        {{ iikiti_embed(url)|raw }}
    {% endif %}
</div>
